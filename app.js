const mayuscula = require('./mayuscula.js');
const minuscula = require('./minuscula.js');

console.log(`Texto: ${mayuscula('Mi primer trabajo en github en mayúscula')}`);
console.log(`Texto: ${minuscula('Mi primer trabajo en github en minúscula')}`);